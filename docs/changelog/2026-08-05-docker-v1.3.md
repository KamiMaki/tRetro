# 2026-08-05 — Docker release v1.3

## Summary

`penguin88428/retroxpert:v1.3` published; `:latest` moved to the same
image. Contains the two feature waves since v1.2:

- Team-only login (daily-password gate removed), password show/hide
  toggle, anonymous-by-default room creation with optional participant
  count. See `docs/changelog/2026-07-25-team-login-and-anonymous-default.md`.
- Interactive discussion: 8 cheer effects with combo escalation,
  presenter-claim focus sync (主持討論/跟隨主持人), card heat glow,
  and the 7-section AI summary prompt with real vote/reaction/metric
  data. See `docs/changelog/2026-08-04-interactive-discussion.md`.

## Image

| Ref | Value |
|---|---|
| Repo | `penguin88428/retroxpert` |
| Tags | `v1.3`, `latest` (same image) |
| Digest | `sha256:c65a4e7513e7cd573a93f1e5818362471d5e80f4646212957e20ed838779a050` |
| Platform | linux/amd64 |

## Verification

- `npx jest`: 31 suites / 325 tests green; `npx tsc --noEmit` exit 0.
- Full Playwright e2e: 27/27 (against a warmed dev server on `E2E_PORT=3100`).
- `npm run build` exit 0; image container smoke: `/api/health` 200,
  unauthenticated `/` → 307 `/login`.
- Remote digest confirmed via `docker buildx imagetools inspect` for
  both tags.

## Rollback

`docker pull penguin88428/retroxpert:v1.2` (digest `a9d92c08…`) —
v1.2 predates the team-only login; it still expects the daily-password
gate. Roll back only with matching expectations for the auth flow.
