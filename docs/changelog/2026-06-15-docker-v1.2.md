# 2026-06-15 — Docker 發佈 v1.2

把含「團隊客製化 + 新手導覽 + UX 修正」批次的 master 打包推送到 Docker Hub。

## 內容
- 來源：master（已 fast-forward 合併 `feat/team-customization`，HEAD `c86aa43`）。
- Image：`penguin88428/retroxpert`，tag **`v1.2`** + `latest`（同一 digest）。
- Manifest list digest：`sha256:a9d92c0826955c5dd4a97a55c54231ddf52b972b6e17dea05f64df7e8a6f62b0`。
- 進版：上一版 v1.1（2026-05-31）→ 本次 **v1.2**（已對 live registry 確認 v1.1 為當時最高）。

## 本版相較 v1.1 的應用變更
- 看板區塊資料化、可自訂（新增/刪除/改名/換色/排序）+ 即時同步。
- 各團隊預設版面、Summary Prompt、常用 emoji 調色盤（團隊設定）。
- 首次進 board 的新手導覽。
- 留言可編輯、圖片過大前置提示 + 點擊放大、討論留言區可拖拉。
- DB migration（開機自動）：移除 `cards.section` CHECK、新增 `room_sections`/
  `team_sections`、`teams.summary_prompt`/`reaction_emojis`、`comments.updated_at`。

## 驗證
- `docker build`：exit 0（多階段，layers OK）。
- 冒煙測試：`docker run` → `/api/health` 回 `{"status":"ok"}`（~4s）；log 顯示
  Database initialized / Socket.IO server initialized / Environment: production。
- `docker push`：v1.2 + latest 成功；遠端 digest == 本機 build manifest list digest。

## 部署提醒
- 既有部署直接 `docker pull penguin88428/retroxpert:v1.2`（或 `latest`）重啟即可；
  schema 由 server 啟動時自動 migrate，資料保留在 `/data` volume。
- 首次升級會在既有 DB 上重建 `cards` 表以移除舊 CHECK（FK-safe、零資料遺失）。
